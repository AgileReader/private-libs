const aolib = require('./aolib');
const datelib = require('./datelib');
const filelib = require('./filelib');
const strlib = require('./strlib');

module.exports = {
  aolib,
  datelib,
  filelib,
  strlib,
};
